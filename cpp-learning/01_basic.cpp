#include <iostream>
#include <cmath>
using namespace std;

int main(){

    cout<<"Hello World!"<<endl;
    
    //comment is used to explain the code and make it more readable. It is ignored by the compiler and does not affect the execution of the program.
    //comment single line
    /*
    comment multiline
    */

    //Syntax is a rule of programming language that defines how programs written in that language should be structured. It includes rules for writing statements, expressions, and other constructs in the language.

    // words vs keywords: In programming, words are the basic building blocks of a program, while keywords are reserved words that have a specific meaning in the programming language. Keywords cannot be used as variable names or identifiers.

    
    
    return 0;
}